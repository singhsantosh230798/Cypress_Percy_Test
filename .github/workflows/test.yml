name: Cypress + Percy Visual Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  # -------------------------------------------------------
  # CYPRESS + PERCY TEST EXECUTION
  # -------------------------------------------------------
  cypress-tests:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        containers: [1, 2]

    env:
      PERCY_TOKEN: ${{ secrets.PERCY_TOKEN }}

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Install dependencies
        run: npm i

      - name: Install Percy CLI
        run: npm install -g @percy/cli

      - name: Run Cypress Tests with Percy
        run: npm run test


  # -------------------------------------------------------
  # FINAL PERCY REVIEW JOB
  # -------------------------------------------------------
  percy-review:
    needs: cypress-tests
    runs-on: ubuntu-latest
    if: always()

    env:
      PERCY_TOKEN: ${{ secrets.PERCY_TOKEN }}
      PERCY_API: "https://percy.io/api/v1"
      TARGET_PROJECT_NAME: "santosh-github-actions2"
      PERCY_USERNAME: ${{ secrets.PERCY_USERNAME }}
      PERCY_ACCESS_KEY: ${{ secrets.PERCY_ACCESS_KEY }}

    steps:
      - name: Install jq
        run: sudo apt-get install -y jq


      # =====================================================
      # 1️⃣ FETCH PROJECT DETAILS
      # =====================================================
      - name: Fetch Percy Project Info
        id: fetch-project
        run: |
          echo "Fetching Percy Project..."

          PROJECT=$(curl -s -H "Authorization: Token $PERCY_TOKEN" "$PERCY_API/projects")
          echo "$PROJECT" | jq -C

          PROJECT_NAME=$(echo "$PROJECT" | jq -r '.data.attributes.name')
          PROJECT_ID=$(echo "$PROJECT" | jq -r '.data.id')

          echo "PROJECT_NAME = $PROJECT_NAME"
          echo "PROJECT_ID   = $PROJECT_ID"

          if [[ "$PROJECT_NAME" != "$TARGET_PROJECT_NAME" ]]; then
            echo "❌ ERROR: Expected project '$TARGET_PROJECT_NAME' but actual is '$PROJECT_NAME'"
            exit 1
          fi

          echo "🎯 Project validated successfully"
          echo "PROJECT_ID=$PROJECT_ID" >> $GITHUB_ENV


      # =====================================================
      # 2️⃣ FETCH LATEST BUILD FOR THIS PROJECT
      # =====================================================
      - name: Fetch Latest Percy Build
        id: fetch-build
        run: |
          echo "Fetching latest builds for project ID: $PROJECT_ID"

          BUILDS=$(curl -s -H "Authorization: Token $PERCY_TOKEN" \
            "$PERCY_API/projects/$PROJECT_ID/builds?per_page=10")

          echo "$BUILDS" | jq -C

          BUILD_ID=$(echo "$BUILDS" | jq -r '.data[0].id')

          echo "🎯 Latest Percy Build ID = $BUILD_ID"
          echo "BUILD_ID=$BUILD_ID" >> $GITHUB_ENV


      # =====================================================
      # 2.5️⃣ WAIT FOR SNAPSHOTS
      # =====================================================
      - name: Wait for Percy snapshots to arrive
        run: |
          echo "⏳ Waiting for snapshots for build $BUILD_ID..."
          MAX_ATTEMPTS=60
          ATTEMPT=1

          while [ $ATTEMPT -le $MAX_ATTEMPTS ]; do
            DATA=$(curl -s -H "Authorization: Token $PERCY_TOKEN" \
              "$PERCY_API/builds/$BUILD_ID")

            STATE=$(echo "$DATA" | jq -r '.data.attributes.state')
            SNAPSHOTS=$(echo "$DATA" | jq -r '.data.attributes["total-snapshots"]')

            echo "Attempt $ATTEMPT → state=$STATE, snapshots=$SNAPSHOTS"

            if [[ "$SNAPSHOTS" != "null" && "$SNAPSHOTS" -gt 0 ]]; then
              echo "📸 Snapshots found: $SNAPSHOTS"
              break
            fi

            ATTEMPT=$((ATTEMPT+1))
            sleep 10
          done

          if [[ "$ATTEMPT" -gt "$MAX_ATTEMPTS" ]]; then
            echo "❌ ERROR: No snapshots received"
            exit 1
          fi


      # =====================================================
      # 2.6️⃣ NEW: WAIT FOR BUILD STATE = FINISHED
      # =====================================================
      - name: Wait for Percy build to reach FINISHED
        run: |
          echo "⏳ Waiting for build to reach state 'finished'..."

          MAX_ATTEMPTS=60
          ATTEMPT=1

          while [ $ATTEMPT -le $MAX_ATTEMPTS ]; do
            STATE=$(curl -s -H "Authorization: Token $PERCY_TOKEN" \
              "$PERCY_API/builds/$BUILD_ID" | jq -r '.data.attributes.state')

            echo "Attempt $ATTEMPT → state=$STATE"

            if [[ "$STATE" == "finished" ]]; then
              echo "🎉 Build finished!"
              break
            fi

            ATTEMPT=$((ATTEMPT+1))
            sleep 10
          done

          if [[ "$STATE" != "finished" ]]; then
            echo "❌ Build never reached FINISHED state"
            exit 1
          fi


      # =====================================================
      # 3️⃣ FETCH SNAPSHOT DETAILS
      # =====================================================
      - name: Fetch Snapshot Details
        id: snapshot-details
        run: |
          echo "=============================="
          echo "📸 Fetching Snapshot Details"
          echo "=============================="

          SNAPSHOTS=$(curl -s -H "Authorization: Token $PERCY_TOKEN" \
            "$PERCY_API/builds/$BUILD_ID/snapshots?per_page=200")

          echo "$SNAPSHOTS" | jq -C

          SNAPSHOT_COUNT=$(echo "$SNAPSHOTS" | jq '.data | length // 0')
          echo "Total Snapshots Found: $SNAPSHOT_COUNT"

          if [[ "$SNAPSHOT_COUNT" -eq 0 ]]; then
            echo "❌ No snapshots to evaluate."
            echo "MAX_DIFF=0" >> $GITHUB_ENV
            exit 0
          fi

          MAX_DIFF=0

          echo "Snapshot Diff Summary:"
          echo "------------------------------------------------"

          while IFS= read -r row; do
            NAME=$(echo "$row" | jq -r '.attributes.name')
            DIFF=$(echo "$row" | jq -r '.attributes["diff-ratio"] // 0')

            printf "Snapshot: %-40s | Diff: %s\n" "$NAME" "$DIFF"

            if (( $(echo "$DIFF > $MAX_DIFF" | bc -l) )); then
              MAX_DIFF=$DIFF
            fi
          done < <(echo "$SNAPSHOTS" | jq -c '.data[]')

          echo "------------------------------------------------"
          echo "📊 MAX DIFF RATIO = $MAX_DIFF"
          echo "------------------------------------------------"

          echo "MAX_DIFF=$MAX_DIFF" >> $GITHUB_ENV
          echo "SNAPSHOT_COUNT=$SNAPSHOT_COUNT" >> $GITHUB_ENV


      # =====================================================
      # 4️⃣ APPROVE OR UNAPPROVE (SAFE, NO CONFLICTS)
      # =====================================================
      - name: Approve or Reject Build
        run: |
          echo "=============================="
          echo "🚦 Final Approval Decision"
          echo "=============================="

          THRESHOLD=0.05

          echo "Build ID:      $BUILD_ID"
          echo "Snapshots:     $SNAPSHOT_COUNT"
          echo "Max Diff:      $MAX_DIFF"
          echo "Threshold:     $THRESHOLD"

          AUTH=$(echo -n "$PERCY_USERNAME:$PERCY_ACCESS_KEY" | base64)

          CURRENT_STATE=$(curl -s -H "Authorization: Token $PERCY_TOKEN" \
            "$PERCY_API/builds/$BUILD_ID" | jq -r '.data.attributes."review-state"')

          echo "Current review state = $CURRENT_STATE"

          approve_build() {
            echo "✔ Attempting APPROVE..."

            if [[ "$CURRENT_STATE" == "approved" ]]; then
              echo "Already approved — skipping"
              return
            fi

            curl -s -X POST "https://percy.io/api/v1/reviews" \
              -H "Authorization: Basic $AUTH" \
              -H "Content-Type: application/json" \
              -d "{
                    \"data\": {
                      \"attributes\": { \"action\": \"approve\" },
                      \"relationships\": { \"build\": { \"data\": { \"type\": \"builds\", \"id\": \"$BUILD_ID\" } } },
                      \"type\": \"reviews\"
                    }
                  }"
          }

          unapprove_build() {
            echo "❌ Attempting UNAPPROVE..."

            if [[ "$CURRENT_STATE" == "unapproved" ]]; then
              echo "Already unapproved — skipping"
              return
            fi

            curl -s -X POST "https://percy.io/api/v1/reviews" \
              -H "Authorization: Basic $AUTH" \
              -H "Content-Type: application/json" \
              -d "{
                    \"data\": {
                      \"attributes\": { \"action\": \"unapprove\" },
                      \"relationships\": { \"build\": { \"data\": { \"type\": \"builds\", \"id\": \"$BUILD_ID\" } } },
                      \"type\": \"reviews\"
                    }
                  }"
          }

          if (( $(echo "$MAX_DIFF <= $THRESHOLD" | bc -l) )); then
            echo "🎉 APPROVING BUILD"
            approve_build
          else
            echo "🚫 UNAPPROVING BUILD"
            unapprove_build
            exit 1
          fi
